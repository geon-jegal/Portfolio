 # StatusUI(2023-05-24 9:00)

    - OnBeginPlay   
	자식에서 함수 실행에 필요한 자식 엔티티들을 받아온다.   

    - OnResize   
	    -  해상도가 바뀔 때 마다 실행되는 함수로 메인 이미지들이 자연스럽게 보이도록 리사이징한다.   
		(사실 카메라 매니저에서 프레임마다 리사이징 하긴 하는데 자식의 자식엔티티로 되어 있어 
		해상도가 바뀔 때 마다 확실하게 하기 위해 한번 더 구현하였다.)   
		main, main_right, main_left, 퀵슬롯, 채팅창이미지가 리사이징 된다.  
	
	- Set함수
		hp, mp, exp게이지의 구현과 채력이 변할 때 마다 이를 반영하는 함수.   
		hp, mp, exp가 변할 때 마다 숫자 이미지도 변하는 데 현재는 이미지들이 중앙이 아닌 왼쪽에 치우치는 이슈가 있음.   
		현재는 OnUpDate함수에서 호출 되지만 이후에는 필요할 때만 호출되도록 변경할 예정   
		(OnUpDate함수 사용을 최대한 지양한다.)
		-> 메인 인터페이스는 자연스러운 움직임이 연출 되어야 하므로 일단은 OnUpDate함수 사용

	- issue   
		게이지가 자연스럽게 움직이도록 구현한 것을 함수로 변경?(호출하는 데 시간이 더 걸리나?)   
		현재는 OnUpDate함수에서 호출 되지만 이후에는 필요할 때만 호출되도록 변경할 예정   
		(OnUpDate함수 사용을 최대한 지양한다.)   
		-> 메인 인터페이스는 자연스러운 움직임이 연출 되어야 하므로 일단은 OnUpDate함수 사용